import 'package:flutter/material.dart';

class PaymentResumePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('PaymentResume'),
        ),
      ),
    );
  }
}
